package stepdefination;

public class StinPLay {

	public void test1() {
		String s="how are you";
		String s1=new String("how are you");
		s.charAt(1);  
		int lastIndex=s.length()-1;
		for(int i=0; i<s.length();i++) {
			System.out.println();
			
			for(int i=0; i<s.length(); i++) {
				for(int j= i+1; j<s.length();j++) {
				s.substring(i)
				
				}
			}
			
			// split method allows u to use space to break it into multiple strings.   String[] =s/split (" ")
		}
	}
	equal is for what object contains if object same it os true
	hashcode if not same there is chance to return same hashcode value
			
	s.substring(4,6);
	
		
			void findduplicate( int[]x)
			{
				for(int i =0;i <x.length; i++) {
					for (int J=i=1; J<x.length; J++>)
					{ if(x[i]== x[J]) {
						System.out.println(x[i] + "is duplicate");
					}}
				}
			}
			
			
			void findHighestNumber(int[]ar) {
				int h= ar[0];
				 for (int i=1; i< ar.length; i ++) {
					 if (ar[i] > h)
					 { h= a[i];}
				 }
			}
			
			int[] x=new int[2];
			x[0]=7;
			x[1]=9;
			for(int i=0; i<x.length; i++)
			{
				System.out.println(x[i]);
			}
			// hw: find the fabonacci number. Swan 2 numbers without declaring a third number value. Find factorial value of a  given number . Find the prime number. Vount the highest hitten number. Find the second highest number
			
}